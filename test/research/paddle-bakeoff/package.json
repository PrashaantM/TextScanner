{
  "name": "textscanner-paddle-bakeoff",
  "private": true,
  "type": "module",
  "description": "BRANCH-ONLY measurement harness for the PaddleOCR bake-off (RECOGNITION-SPIKE.md §5). Not part of the app, not wired into ci.yml. See README.md.",
  "scripts": {
    "accuracy": "node score-paddle.mjs",
    "browser": "node browser-bakeoff.mjs"
  },
  "dependencies": {
    "onnxruntime-node": "1.30.0",
    "onnxruntime-web": "1.30.0",
    "ppu-paddle-ocr": "6.6.0"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1"
  }
}
